version: '3.4'

services:
  postgres:
    image: postgres:13-alpine
    environment:
      - ${POSTGRES_CONNECTION_URL}/${POSTGRES_DATABASE}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
      - /tmp/devday/postgres:/tmp
    restart: "always"
    environment:
      POSTGRES_PASSWORD: postgrespassword
