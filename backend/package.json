{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon -w src --exec node src/index.js",
    "start": "node src/index.js",
    "build": "echo TODO: do we need build?",
    "migrations": "DATABASE_URL=$ROOT_DATABASE_URL? graphile-migrate",
    "migrations-check": "DATABASE_URL=$ROOT_DATABASE_URL? node src/check-migrations.js",
    "init-schemas": "DATABASE_URL=$ROOT_DATABASE_URL? node src/init-schemas.js"
  },
  "dependencies": {
    "@graphile-contrib/pg-simplify-inflector": "^6.1.0",
    "graphile-migrate": "^1.0.2",
    "graphql-type-json": "^0.3.2",
    "lodash": "^4.17.21",
    "node-fetch": "^2.6.1",
    "polka": "^0.5.2",
    "postgraphile": "^4.11.0",
    "postgraphile-plugin-connection-filter": "^2.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
